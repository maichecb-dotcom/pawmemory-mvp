{
  "name": "pawmemory-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "PawMemory MVP web app and Node server for Tencent Cloud deployment.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check api/chat.js && node --check api/config.js && node --check api/profile.js && node --check netlify/functions/chat.js"
  },
  "engines": {
    "node": ">=18"
  }
}
